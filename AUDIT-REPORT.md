# تقرير مراجعة وإصلاح منصة مجتمعنا

## نطاق المراجعة

تمت مراجعة الحزمة المرفقة التي تحتوي على واجهة المنصة وملفات JavaScript الخاصة بالمصادقة، المحادثات، المجتمعات، المكافآت، ومكالمات الفيديو. تم تشغيل فحص صياغة لملفات JavaScript بعد التعديل.

## الإصلاحات المنفذة

| الملف أو المكوّن | الإجراء |
|---|---|
| `cpx-rewards.js` | إزالة مفتاح CPX السري وحساب التوقيع من كود المتصفح. أصبح إنشاء رابط العرض يمر عبر `/api/cpx-offerwall`. |
| `api/cpx-offerwall.js` | إضافة API خادمي ينشئ رابط CPX باستخدام متغيرات بيئة سرية. |
| `api/cpx-postback.js` | إضافة endpoint لمعالجة CPX postback والتحقق من الهاش وتسجيل المعاملة وإضافة النقاط. توجد حماية أساسية من إعادة استخدام `transaction_id`. |
| `video-call.js` | إزالة مفتاح Metered من الواجهة وإيقاف إنشاء غرف الفيديو مؤقتًا بدل إبقاء المفتاح مكشوفًا. |
| `.env.example` | توثيق المتغيرات السرية المطلوبة للتشغيل. |
| `SECURITY.md` | توثيق المخاطر العاجلة ومتطلبات النشر الآمن. |

## مشكلات حرجة لم تُحل آليًا

### كلمات المرور

`auth.js` يخزن كلمات المرور كنص صريح داخل Realtime Database ويقارنها من المتصفح. هذا غير مناسب للإنتاج. يجب نقل الحسابات إلى Firebase Authentication أو Backend يستخدم Argon2id أو bcrypt، ثم ترحيل المستخدمين وإلغاء حقل `password` القديم.

### قواعد Firebase

لا تحتوي الحزمة على قواعد Realtime Database أو Storage. لا يمكن إنشاء قواعد آمنة من دون معرفة مخطط البيانات الكامل ومتطلبات القراءة والكتابة. يجب منع المستخدم من تعديل `points` و`pointsHistory` و`rewardTransactions` وبيانات الحسابات الحساسة مباشرة من المتصفح.

### مفتاح Metered

يجب تدوير المفتاح الذي ظهر في النسخة المنشورة. إعادة تفعيل المكالمات تتطلب endpoint خادميًا يتحقق من هوية المستخدم وعضويته في المجتمع قبل طلب إنشاء غرفة.

### نظام CPX

يجب تدوير مفتاح CPX الذي ظهر في النسخة المنشورة، ووضع المفتاح الجديد في إعدادات Vercel كمتغير سري. كما يجب ضبط رابط postback الفعلي في CPX Dashboard، وتوفير `FIREBASE_DATABASE_SECRET` للخادم فقط. اختبر إعادة إرسال نفس المعاملة قبل فتح السحب للمستخدمين.

## إعدادات Vercel المطلوبة

```text
CPX_APP_ID=35568
CPX_SECRET_KEY=<المفتاح الجديد بعد التدوير>
REWARDS_USER_SHARE=0.70
FIREBASE_DATABASE_URL=https://<project>-default-rtdb.firebaseio.com
FIREBASE_DATABASE_SECRET=<سر خادمي فقط>
```

لا تضع هذه القيم في الملفات العامة أو في JavaScript الواجهة.

## الاختبار المنفذ

تم تشغيل:

```text
node --check api/cpx-offerwall.js
node --check api/cpx-postback.js
node --check video-call.js
node --check cpx-rewards.js
```

واجتازت الملفات الأربعة فحص الصياغة دون أخطاء.

## ترتيب المرحلة التالية

1. ترحيل المصادقة من نظام كلمات المرور المخصص إلى Firebase Authentication.
2. إضافة قواعد Firebase ومراجعتها مع كل مسار بيانات.
3. بناء API مكالمات خادمي بعد اكتمال المصادقة.
4. إضافة محفظة وسحب بحالات `pending`, `confirmed`, `rejected`, `reversed`.
5. استخدام معاملة ذرية أو Cloud Function لمنع سباق postback المتزامن.
6. إضافة سياسة الخصوصية وشروط الاستخدام وسياسة المكافآت والسحب.

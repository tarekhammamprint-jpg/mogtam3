# ملاحظات الأمان قبل النشر

## عاجل

1. تم تسريب مفتاح CPX ومفتاح Metered في النسخة المنشورة. يجب تدوير المفتاحين من لوحتي الخدمتين قبل إعادة النشر.
2. كلمات المرور تُخزّن حاليًا داخل Realtime Database كنص صريح عبر `auth.js`. لا تُستخدم هذه المصادقة في الإنتاج. يجب نقل الحسابات إلى Firebase Authentication أو Backend يستخدم Argon2id/bcrypt، ثم إبطال حقل `password` القديم بعد ترحيل المستخدمين.
3. لا توجد قواعد Firebase ضمن الحزمة. لا تنشر المشروع قبل إضافة قواعد تمنع المستخدم من تعديل رصيده أو نقاطه أو سجل المكافآت مباشرة من المتصفح.

## CPX

تم نقل إنشاء رابط Offerwall ومعالجة postback إلى `api/`. ضع القيم الموجودة في `.env.example` كمتغيرات سرية في Vercel. يجب ضبط رابط CPX postback على endpoint النشر ثم اختبار معاملة واحدة واختبار إعادة إرسال نفس `transaction_id`.

الـ idempotency في endpoint يمنع التكرار الأساسي، لكن يفضل لاحقًا استخدام معاملة ذرية أو Cloud Function/Firestore transaction لمنع سباق الطلبات المتزامنة.

## Metered

`video-call.js` يحتوي مفتاح Metered داخل JavaScript العام. لا تعِد تفعيل إنشاء الغرف من المتصفح. انقل استدعاء Metered إلى API خادمي بعد إضافة Firebase Authentication والتحقق من عضوية المستخدم في المجتمع على الخادم.

## النشر

- دوّر جميع الأسرار التي ظهرت في Git أو Vercel أو المتصفح.
- فعّل HTTPS فقط.
- أضف سياسة خصوصية وشروط استخدام وسياسة سحب.
- اختبر السحب والعمليات المرتجعة يدويًا قبل فتحه للمستخدمين.
